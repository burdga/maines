git clone https://github.com/burdga/maines
git branch
git add file name
git branch
git commit -m "comment"
git config --global user.name "burdga"
git config --global user.email suxoza@yandex.ru
git push -u origin master
git branch new_folder
git checkout new_folder

